[gd_scene load_steps=4 format=3 uid="uid://cruqwfrsgioe2"]

[ext_resource type="Texture2D" uid="uid://dpqaycl5uqy1m" path="res://assets/kenney_scribble-platformer/PNG/Retina/character_squareGreen.png" id="1_adnrf"]

[sub_resource type="GDScript" id="GDScript_nv0l7"]
script/source = "extends CharacterBody2D

const SPEED = 300.0
const JUMP_VELOCITY = -600.0

# Get the gravity from the project settings to be synced with RigidBody nodes.
var gravity = ProjectSettings.get_setting(\"physics/2d/default_gravity\")


func _physics_process(delta):
	# Add the gravity.
	if not is_on_floor():
		velocity.y += gravity * delta

	# Handle Jump.
	if Input.is_action_just_pressed(\"ui_accept\") and is_on_floor():
		velocity.y = JUMP_VELOCITY

	# Get the input direction and handle the movement/deceleration.
	# As good practice, you should replace UI actions with custom gameplay actions.
	var direction = Input.get_axis(\"ui_left\", \"ui_right\")
	if direction:
		velocity.x = direction * SPEED
	else:
		velocity.x = move_toward(velocity.x, 0, SPEED)

	move_and_slide()
"

[sub_resource type="RectangleShape2D" id="RectangleShape2D_iq1q1"]
size = Vector2(79, 136)

[node name="Player" type="CharacterBody2D"]
script = SubResource("GDScript_nv0l7")

[node name="Sprite2D" type="Sprite2D" parent="."]
position = Vector2(0, -64)
texture = ExtResource("1_adnrf")

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
position = Vector2(-0.5, -58)
shape = SubResource("RectangleShape2D_iq1q1")

[node name="Camera2D" type="Camera2D" parent="."]
position = Vector2(0, -187)
